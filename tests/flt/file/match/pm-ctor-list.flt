// Test: constructor containing list, with nested destructuring
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
type Opt 'a =
    | None
    | Some of 'a
let result =
    match Some [1; 2; 3] with
    | Some (x :: _) -> x
    | Some [] -> 0
    | None -> 0
// --- Output:
1
