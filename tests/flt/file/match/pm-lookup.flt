// Test: association list lookup with pattern matching
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let result =
    let rec lookup key = fun xs ->
        match xs with
        | [] -> None
        | (k, v) :: rest -> if k = key then Some v else lookup key rest
    let env = [(1, "one"); (2, "two"); (3, "three")]
    let r1 = lookup 2 env
    let r2 = lookup 9 env
    (r1, r2)
// --- Output:
(Some "two", None)
