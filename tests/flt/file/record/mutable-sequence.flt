// Test: mutable record field mutation sequence
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
type Counter = { mutable count: int }

let c = { count = 0 }
let _ = c.count <- 1
let _ = c.count <- c.count + 1
let _ = c.count <- c.count + 1
let result = c.count
// --- Output:
3
