// Test: multiline lambda body after fun ->
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let f = fun xs ->
    match xs with
    | [] -> 0
    | h :: _ -> h

let result = f [42; 99]
// --- Output:
42
