// Test IFTHEN-01+SEQ: if-then without else with semicolon-sequenced then-branch
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let x = 3
let _ = if x > 0 then println "a"; println "b"
// --- Output:
a
b
()
