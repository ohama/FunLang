// Test: recursive exponentiation
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let rec power base = fun exp ->
        if exp = 0 then 1 else base * power base (exp - 1)
    power 2 10
// --- Output:
1024
