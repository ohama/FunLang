// Test: 7-level deep nesting stress test with mixed constructs
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let a =
        let b =
            if true then
                let c =
                    match 1 with
                    | 1 ->
                        let d =
                            let e =
                                let f =
                                    let g = 42
                                    g
                                f
                            e
                        d
                    | _ -> 0
                c
            else 0
        b
    a
// --- Output:
42
