---
phase: 102-fix-lambdaannot-span-collision
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - tests/FunLang.Tests/TypeAnnotationTests.fs
  - tests/flt/type-annot/multi-param-span/run.fun
  - tests/flt/type-annot/multi-param-span/expected
autonomous: true

must_haves:
  truths:
    - "Multi-param annotated function has distinct annotationMap entries per LambdaAnnot"
    - "Each parameter's annotationMap entry holds the correct arrow type for that nesting level"
    - "Regression test confirms the bug is fixed end-to-end"
  artifacts:
    - path: "tests/FunLang.Tests/TypeAnnotationTests.fs"
      provides: "TA-08 unit test for multi-param LambdaAnnot span uniqueness"
      contains: "TA-08"
    - path: "tests/flt/type-annot/multi-param-span/run.fun"
      provides: "flt regression test for Issue #18"
  key_links:
    - from: "TA-08 test"
      to: "annotationMap"
      via: "typeCheckAndSnapshot"
      pattern: "typeCheckAndSnapshot"
---

<objective>
Add unit test and flt regression test proving the LambdaAnnot span collision bug (Issue #18) is fixed.

Purpose: Verify that each nested LambdaAnnot gets a distinct span and the annotationMap maps each span to the correct arrow type for that parameter's nesting level.

Output: New TA-08 unit test in TypeAnnotationTests.fs and new flt regression test directory.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/102-fix-lambdaannot-span-collision/102-01-SUMMARY.md
@tests/FunLang.Tests/TypeAnnotationTests.fs
@src/FunLang/Ast.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TA-08 unit test for multi-param LambdaAnnot span uniqueness</name>
  <files>tests/FunLang.Tests/TypeAnnotationTests.fs</files>
  <action>
Add a new test `TA-08` to the `typeAnnotationTests` test list in TypeAnnotationTests.fs.

The test should:
1. Parse and type-check a multi-param annotated function:
   `let f (x : int) (y : string) (z : bool) : int = 42`
2. Collect all LambdaAnnot spans from the parsed AST (write a small helper or walk the AST).
3. Assert that the number of distinct LambdaAnnot spans equals the number of annotated parameters (3 in this case).
4. Assert that the annotationMap contains entries for each distinct span.
5. Assert that the arrow types at each span are different:
   - Outermost: `int -> string -> bool -> int`
   - Middle: `string -> bool -> int`
   - Innermost: `bool -> int`

Strategy for finding LambdaAnnot spans: Walk the AST from the parsed module's top-level binding to find nested LambdaAnnot nodes and collect their spans. Use `collectSpans` style but specifically for LambdaAnnot nodes:
```fsharp
let rec collectLambdaAnnotSpans (expr: Ast.Expr) : Ast.Span list =
    match expr with
    | Ast.LambdaAnnot(_, _, body, sp) -> sp :: collectLambdaAnnotSpans body
    | _ -> []
```

Then verify:
- `spans |> List.distinct |> List.length` equals `List.length spans` (all unique)
- Each span exists as a key in the annotationMap snapshot
- The types at each span are TArrow with decreasing arity
  </action>
  <verify>
Run: `dotnet test tests/FunLang.Tests/FunLang.Tests.fsproj -c Release --filter "TA-08"` -- test passes.
Run: `dotnet test tests/FunLang.Tests/FunLang.Tests.fsproj -c Release` -- all tests pass (245 total).
  </verify>
  <done>
TA-08 test passes, confirming 3 distinct spans for 3 annotated parameters, each with the correct arrow type in annotationMap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add flt regression test for multi-param annotated function</name>
  <files>tests/flt/type-annot/multi-param-span/run.fun, tests/flt/type-annot/multi-param-span/expected</files>
  <action>
Create a new flt test directory `tests/flt/type-annot/multi-param-span/` with:

1. `run.fun`: A program that defines a multi-param annotated function and calls it, printing the result. This exercises the fix end-to-end (parsing -> type checking -> evaluation):
   ```
   let add (x : int) (y : int) : int = x + y
   let result = add 3 4
   printfn "%d" result
   ```
   Expected output: `7`

2. `expected`: The expected output file containing `7` followed by a newline.

Also create a second test for mixed params (plain + annotated) if directory structure allows:
- `tests/flt/type-annot/mixed-param-span/run.fun`:
  ```
  let greet (name : string) greeting : string = greeting ^ ", " ^ name ^ "!"
  printfn "%s" (greet "world" "hello")
  ```
- `tests/flt/type-annot/mixed-param-span/expected`: `hello, world!`

Check existing flt test directory structure first to match conventions (look at `tests/flt/` for naming patterns and whether `expected` files use trailing newlines).
  </action>
  <verify>
Run: `scripts/fslit tests/flt/type-annot/multi-param-span/` -- passes.
Run: `scripts/fslit tests/flt/type-annot/mixed-param-span/` -- passes (if created).
Run: `scripts/fslit tests/flt/` -- all tests pass (724 + new tests).
Run: `dotnet test tests/FunLang.Tests/FunLang.Tests.fsproj -c Release` -- all unit tests pass.
  </verify>
  <done>
New flt regression tests pass, confirming multi-param annotated and mixed-param functions work correctly end-to-end after the span fix.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/FunLang.Tests/FunLang.Tests.fsproj -c Release` -- all tests pass (245+)
2. `scripts/fslit tests/flt/` -- all tests pass (726+)
3. TA-08 specifically verifies distinct spans and correct arrow types per LambdaAnnot
</verification>

<success_criteria>
- TA-08 unit test passes: 3 distinct spans, 3 distinct arrow types in annotationMap
- flt regression tests pass: multi-param annotated function evaluates correctly
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/102-fix-lambdaannot-span-collision/102-02-SUMMARY.md`
</output>
