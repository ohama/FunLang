// Test: nested record access with chained dot notation
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
type Inner = { value: int }
type Outer = { inner: Inner; name: string }

let o = { inner = { value = 42 }; name = "test" }
let result = o.inner.value
// --- Output:
42
