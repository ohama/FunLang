// Test: complex pipe chain with multiple functions
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let double x = x * 2
let inc x = x + 1
let square x = x * x
let result = 3 |> inc |> double |> square
// --- Output:
64
