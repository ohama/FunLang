# F# Interactive (FSI)

Run F# code interactively using F# Interactive.

## Arguments

- `$ARGUMENTS` - Optional: .fsx script file to run

## Steps

1. If script provided, run `dotnet fsi <script.fsx>`
2. If no script, launch `dotnet fsi` for interactive session
3. Display output and any errors
