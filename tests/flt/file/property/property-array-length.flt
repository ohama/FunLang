// Test: .Length property on arrays
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let arr = Array.create 3 0
let n = Array.length arr
let _ = println (to_string n)
let arr2 = Array.create 5 "x"
let _ = println (to_string (Array.length arr2))
// --- Stdout:
3
5
