// Test: parametric ADT (Option type) with type variable
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
type Option 'a =
    | Some of 'a
    | None

let getOrDefault o = match o with
| Some x -> x
| None -> 0

let result = getOrDefault (Some 42)
// --- Output:
42
