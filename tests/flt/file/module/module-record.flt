// Test: module containing record type and operations
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
module Geo =
    type Point = { x: int; y: int }
    let origin = { x = 0; y = 0 }

open Geo
let p = { x = 3; y = 4 }
let result = p.x + p.y
// --- Output:
7
