// Test: Array.fold with string accumulator
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let arr = Array.ofList ["a"; "b"; "c"]
let result = Array.fold (fun acc -> fun x -> acc ^^ x) "" arr
let _ = println result
// --- Output:
abc
