// Test: tail-recursive loop with 1M iterations (TCO prevents stack overflow)
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let rec loop n = if n = 0 then 0 else loop (n - 1)
    loop 1000000
// --- Output:
0
