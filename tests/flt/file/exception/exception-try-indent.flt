// Test: try-with with indented body
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
exception MyError

let result =
    try
        raise MyError
    with
    | MyError -> 42
// --- Stdout:
42
