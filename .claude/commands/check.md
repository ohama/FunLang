# Check Project

Perform a full project check: restore, build, and test.

## Steps

1. Run `dotnet restore` to ensure dependencies are up to date
2. Run `dotnet build` to compile the project
3. Run `dotnet test` to execute tests
4. Report overall status and any issues found
