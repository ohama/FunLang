// Test: when guards for range classification
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let grade score =
    match score with
    | s when s >= 90 -> "A"
    | s when s >= 80 -> "B"
    | s when s >= 70 -> "C"
    | s when s >= 60 -> "D"
    | _ -> "F"
let result = grade 85
// --- Output:
"B"
