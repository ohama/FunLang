// Test: match nested inside deep let bindings (depth 5)
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let result =
    let x = 10
    let y =
        let z =
            let w =
                match x with
                | 10 -> 100
                | _ -> 0
            w + 1
        z + 1
    y + 1
// --- Output:
103
