// Test: HashSet with string elements (COLL-02)
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let hs = HashSet.create ()
let _ = HashSet.add hs "apple"
let _ = HashSet.add hs "banana"
let _ = HashSet.add hs "apple"
let _ = println (to_string (HashSet.count hs))
let _ = println (to_string (HashSet.contains hs "apple"))
let _ = println (to_string (HashSet.contains hs "cherry"))
// --- Output:
2
true
false
()
