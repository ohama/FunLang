// Test: length of a list
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result = let rec length xs = match xs with | [] -> 0 | _ :: rest -> 1 + length rest in length [1; 2; 3]
// --- Output:
3
