// Test: parenthesized patterns in match (uses Prelude Option)
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let x = Some 42
let result =
    match x with
    | (Some v) -> v
    | None -> 0
// --- Stdout:
42
