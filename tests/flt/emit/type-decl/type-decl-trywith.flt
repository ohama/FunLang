// Test: --emit-type for try-with exception handling
// --- Command: src/FunLang/bin/Release/net10.0/FunLang --emit-type %input
// --- Input:
exception NotFound

let safe f x =
    try
        f x
    with
    | NotFound -> 0
    | _ -> 0
// --- Output:
NotFound : exn
safe : ('a -> int) -> 'a -> int
