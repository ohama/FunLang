// Test: for-in iteration over HashSet (LANG-03)
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let hs = HashSet.create ()
let _ = HashSet.add hs 42
let _ = for x in hs do println (to_string x)
// --- Output:
42
()
