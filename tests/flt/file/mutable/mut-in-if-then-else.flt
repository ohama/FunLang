// Test: let mut used with if-then-else
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let mut x = 0
    let _ = if true then x <- 100 else x <- 200
    x
// --- Output:
100
