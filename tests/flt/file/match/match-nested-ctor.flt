// Test: nested constructor patterns (uses Prelude Option)
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let x = Some (Some 42)
let result =
    match x with
    | Some (Some v) -> v
    | _ -> 0
// --- Stdout:
42
