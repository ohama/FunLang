// Test IFTHEN-01: if-then without else accepted when then-branch returns unit
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let x = 5
let _ = if x > 0 then println "positive"
// --- Output:
positive
()
