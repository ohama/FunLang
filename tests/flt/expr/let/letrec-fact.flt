// Test: recursive factorial with let rec
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let result = let rec fact n = if n <= 1 then 1 else n * fact (n - 1) in fact 5
// --- Output:
120
