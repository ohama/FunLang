// Test: tuple type alias (type IntPair = int * int) with destructuring
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
type IntPair = int * int
let p = (3, 7)
let result = match p with | (a, b) -> a + b
// --- Output:
10
