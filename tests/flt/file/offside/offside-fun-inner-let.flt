// Test: implicit in inside fun -> body
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let f = fun x ->
        let y = x + 1
        y * 2
    f 5
// --- Output:
12
