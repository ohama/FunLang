// Test: nested tuple pattern matching
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let classify pair = match pair with
| (0, 0) -> "origin"
| (0, _) -> "y-axis"
| (_, 0) -> "x-axis"
| _ -> "other"

let result = classify (0, 5)
// --- Output:
"y-axis"
