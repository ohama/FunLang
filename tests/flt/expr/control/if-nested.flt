// Test: nested if-then-else with strings
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let x =
    if 1 > 2 then "a"
    else if 2 > 3 then "b"
    else "c"
// --- Output:
"c"
