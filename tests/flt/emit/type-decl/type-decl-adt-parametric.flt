// Test: --emit-type for parametric ADT type inference
// --- Command: src/FunLang/bin/Release/net10.0/fn --emit-type %input
// --- Input:
type Option 'a =
    | Some of 'a
    | None

let x = Some 42

let y = None
// --- Output:
x : Option<int>
y : Option<'a>
