// Test: --emit-type for record field access and copy-update
// --- Command: src/FunLang/bin/Release/net10.0/FunLang --emit-type %input
// --- Input:
type Point = { x: int; y: int }

let p = { x = 1; y = 2 }
let v = p.x
let q = { p with y = 10 }
// --- Output:
p : Point
q : Point
v : int
