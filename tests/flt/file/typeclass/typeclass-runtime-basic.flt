// Test: instance Show int — show 42 evaluates to "42" at runtime
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
typeclass Show 'a =
    | show : 'a -> string

instance Show int =
    let show x = to_string x

let result = show 42
// --- Stdout:
"42"
