// Test: qualified access to module members
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
module Math =
    let add x y = x + y

let result = Math.add 3 4
// --- Output:
7
