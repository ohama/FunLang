// Test: namespace with type and function declarations
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
namespace App

type Color =
    | Red
    | Green
    | Blue

let pick c = match c with
| Red -> 1
| Green -> 2
| Blue -> 3

let result = pick Blue
// --- Output:
3
