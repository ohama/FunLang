# Clean Project

Clean all build artifacts and intermediate files.

## Steps

1. Run `dotnet clean` to remove build outputs
2. Optionally remove obj/ and bin/ directories if requested
3. Report cleanup status
