// Test: let inside if branch
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    if true then
        let x = 10
        let y = 20
        x + y
    else 0
// --- Output:
30
