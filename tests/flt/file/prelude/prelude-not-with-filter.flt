// Test: not with filter for negation
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let isEven x = x - (x / 2) * 2 = 0
let odds = filter (fun x -> not (isEven x)) [1..10]
let result = odds
// --- Output:
[1; 3; 5; 7; 9]
