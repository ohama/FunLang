// Test: typeclass declaration with multiple methods parses both to TypeClassDecl methods list
// --- Command: src/FunLang/bin/Release/net10.0/fn --emit-ast %input
// --- Input:
typeclass Eq 'a =
    | eq : 'a -> 'a -> bool
    | neq : 'a -> 'a -> bool
// --- Output:
TypeClassDecl "Eq" 'a [eq : TEArrow (TEVar "'a", TEArrow (TEVar "'a", TEBool)), neq : TEArrow (TEVar "'a", TEArrow (TEVar "'a", TEBool))]
