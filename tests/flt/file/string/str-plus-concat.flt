// Test: string concatenation with + operator in file mode
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let a = "hello"
let b = " "
let c = "world"
let result = a + b + c
// --- Output:
"hello world"
