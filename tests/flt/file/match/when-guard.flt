// Test: when guards in match expressions
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result = match 5 with
| x when x > 3 -> "big"
| _ -> "small"
// --- Output:
"big"
