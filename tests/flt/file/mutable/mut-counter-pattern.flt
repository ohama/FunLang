// Test: mutable counter pattern — increment via closure
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let mut count = 0
let inc x = count <- count + 1
let _ = inc 0
let _ = inc 0
let _ = inc 0
let _ = println (to_string count)
// --- Output:
3
