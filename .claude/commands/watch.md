# Watch Mode

Run the project in watch mode for automatic rebuilding on file changes.

## Steps

1. Run `dotnet watch run` in the project directory
2. Monitor the output for build errors
3. Report status to the user
