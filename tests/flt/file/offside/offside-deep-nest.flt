// Test: deeply nested let blocks (3 levels)
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let a =
        let b =
            let c = 42
            c + 1
        b + 1
    a + 1
// --- Output:
45
