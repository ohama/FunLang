// Test: Prelude Option with pattern matching
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let getOrDefault opt def = match opt with
| Some x -> x
| None -> def

let result = getOrDefault (Some 42) 0
// --- Output:
42
