// Test: nested module declarations with deep qualified access
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
module Outer =
    module Inner =
        let x = 42

let result = Outer.Inner.x
// --- Output:
42
