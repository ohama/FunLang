# Format Code

Format F# source files using Fantomas (if installed).

## Steps

1. Check if Fantomas is available (`dotnet tool list`)
2. If not installed, suggest installing with `dotnet tool install fantomas`
3. Run `dotnet fantomas .` to format all F# files
4. Report any formatting changes made
