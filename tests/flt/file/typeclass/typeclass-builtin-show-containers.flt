// Test: built-in Show for bool and char — show true and show 'x' work without user instance
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let a = show true
let b = show 'x'
let result = show "hello"
// --- Stdout:
"hello"
