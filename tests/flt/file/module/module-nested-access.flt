// Test: nested module with qualified access chain
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
module Outer =
    module Inner =
        let value = 42

let result = Outer.Inner.value
// --- Output:
42
