// Test: single-line try-with as let RHS with implicit in
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
exception E

let result =
    let x = 42
    let y = try raise E with | E -> 0
    x + y
// --- Output:
42
