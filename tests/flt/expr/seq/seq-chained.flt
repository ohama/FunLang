// Test SEQ-02: e1; e2; e3 chains correctly (right-associative), returning value of last expr
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let result = let mut x = 0 in x <- 1; x <- x + 1; x <- x + 1; x
// --- Output:
3
