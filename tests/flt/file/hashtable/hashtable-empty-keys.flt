// Test: Empty hashtable - keys returns empty list
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let ht = Hashtable.create ()
let ks = Hashtable.keys ht
let _ = println (to_string (List.length ks))
// --- Output:
0
