// Test: nested patterns in match (tuple containing ADT)
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
type Wrapper = Wrap of int

let result = match (Wrap 5, true) with
| (Wrap n, true) -> n
| (Wrap n, false) -> 0
// --- Output:
5
