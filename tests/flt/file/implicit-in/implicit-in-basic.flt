// Test: implicit in — F#-style let sequence without explicit in
// --- Command: src/FunLang/bin/Release/net10.0/FunLang %input
// --- Input:
let result =
    let x = 1
    let y = 2
    x + y
// --- Output:
3
