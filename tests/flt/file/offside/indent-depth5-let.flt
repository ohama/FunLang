// Test: 5-level deep nested let bindings with implicit in
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let a =
        let b =
            let c =
                let d =
                    let e = 1
                    e + 1
                d + 1
            c + 1
        b + 1
    a + 1
// --- Output:
6
