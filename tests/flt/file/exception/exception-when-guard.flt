// Test: exception handler with when guard
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
exception MyError of int

let result = try
    raise (MyError 5)
with
| MyError x when x > 0 -> "positive"
| MyError _ -> "other"
// --- Output:
"positive"
