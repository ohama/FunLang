// Test: implicit in — three let bindings
// --- Command: src/FunLang/bin/Release/net10.0/fn %input
// --- Input:
let result =
    let a = 10
    let b = 20
    let c = 30
    a + b + c
// --- Output:
60
